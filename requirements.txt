# slidingLLM runtime, pinned from the working hj_svdllm env (python 3.9.25).
#
# torch is NOT listed here on purpose: it needs the CUDA-matched wheel index.
# Install it first, separately:
#
#   pip install torch==2.8.0 torchvision==0.23.0 \
#       --index-url https://download.pytorch.org/whl/cu128
#
# Swap cu128 for whatever the target box's driver supports.

transformers==4.35.2
tokenizers==0.15.2
huggingface_hub==0.36.2
safetensors==0.7.0
accelerate==1.10.1
datasets==2.16.1
pyarrow==21.0.0
sentencepiece==0.1.99
protobuf==6.33.6

numpy==1.26.3
pandas==2.3.3
tqdm==4.65.0
matplotlib==3.4.3
pillow==11.3.0

# only used by the FLOP counting / baseline paths, cheap to keep
thop==0.1.1.post2209072238
timm==1.0.28
